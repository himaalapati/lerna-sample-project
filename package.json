{
  "name": "ldemo",
  "version":"0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
